# cargo-deny policy: https://embarkstudios.github.io/cargo-deny/

[graph]
all-features = true

[advisories]
yanked = "deny"
# Each ignore needs a reason and should be removed once the upstream path is gone.
ignore = [
    { id = "RUSTSEC-2025-0141", reason = "bincode unmaintained; pulled by argmin 0.8, remove after argmin upgrade" },
    { id = "RUSTSEC-2024-0384", reason = "instant unmaintained; pulled by argmin 0.8, remove after argmin upgrade" },
    { id = "RUSTSEC-2024-0436", reason = "paste unmaintained; pulled by argmin 0.8 and nalgebra/simba via statrs 0.16" },
]

[licenses]
# Every license in the dependency graph must be on this list. Adding one is a
# deliberate decision; note why in the PR that changes it.
allow = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "MIT",
    "MPL-2.0",
    "Unicode-3.0",
    "Unlicense",
    "Zlib",
]
confidence-threshold = 0.9

[bans]
multiple-versions = "warn"
wildcards = "deny"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
